name: shared_contracts
description: Shared contracts and DTOs across feature packages.
publish_to: 'none'
version: 0.1.0
resolution: workspace

environment:
  sdk: '>=3.11.0 <4.0.0'

dependencies:
  meta: ^1.16.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.3
